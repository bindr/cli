{
  "name": "@bindr/cli",
  "version": "0.0.3",
  "description": "(WIP) CLI tool for Bindr.",
  "author": "cypherix93",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bindr/cli.git"
  },
  "homepage": "https://github.com/bindr/cli#readme",
  "bugs": {
    "url": "https://github.com/bindr/cli/issues"
  },
  "keywords": [
    "bindr",
    "cli",
    "markdown",
    "documentation"
  ],
  "license": "UNLICENSED",
  "main": "./dist/index.js",
  "bin": {
    "bindr": "./dist/index.js"
  },
  "engines": {
    "node": ">=6.5.0"
  },
  "files": [
    "dist/"
  ],
  "dependencies": {
    "directory-tree": "^2.0.0",
    "yargs": "^8.0.2"
  },
  "devDependencies": {
    "@types/node": "^8.0.4",
    "@types/yargs": "^8.0.0",
    "concurrently": "^3.5.0",
    "cross-env": "^5.0.1",
    "rimraf": "^2.6.1",
    "tslint": "^5.1.0",
    "typescript": "^2.3.4"
  },
  "scripts": {
    "clean": "rimraf ./dist/",
    "build": "npm run clean & npm run compile",
    "compile": "tsc",
    "compile:watch": "tsc --watch",
    "prepack": "npm run build"
  }
}
